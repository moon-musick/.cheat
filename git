# vim: ft=sh ts=2:sts=2:sw=2:et

# find first occurence of (fixed) string in history
git log -S <string> --source --all

# delete remote branch
git push REMOTE --delete BRANCH_NAME
# e.g.
git push origin --delete some_feature_branch
